# Moved

The Pebble Glucose Protocol now lives in its own repository:

**https://github.com/mortenfyhn/pebble-glucose-protocol**

It lived here while this watchface and one bridge app were its only implementations. It moved out so it
isn't owned by any single implementation — this watchface is the protocol's reference implementation,
not its author.

This stub stays because the old path is linked from elsewhere.
